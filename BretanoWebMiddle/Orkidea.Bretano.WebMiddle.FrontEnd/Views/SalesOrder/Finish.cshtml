
@{
    ViewBag.Title = "Pedido";
}

<div class="panel panel-@ViewBag.colorMensaje">
    <div class="panel-heading">
        <h3 class="panel-title">@ViewBag.mensaje</h3>
    </div>
    <div class="panel-body">
        @ViewBag.docEntry
        <br />
        @Html.ActionLink("Aceptar", "Create", "SalesOrder", new { id = ViewBag.id }, new { @class = "btn btn-success" })
    </div>
</div>

